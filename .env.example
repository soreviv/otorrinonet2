# Variables para docker-compose.yml (raíz del repo).
# Copiar a ".env" en esta misma carpeta y rellenar. Nunca commitear ".env".
#
# Generar una contraseña fuerte, por ejemplo:
#   openssl rand -base64 32

POSTGRES_USER=otorrinonet
POSTGRES_PASSWORD=
POSTGRES_DB=otorrinonet
